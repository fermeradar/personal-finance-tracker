@@ -108,8 +108,9 @@
+    let startDate, endDate;
     switch (period) {
       case 'week':
-        let startDate = moment().subtract(1, 'week').startOf('day');
-        let endDate = moment().endOf('day');
+        startDate = moment().subtract(1, 'week').startOf('day');
+        endDate = moment().endOf('day');
         break;
 
 
 

--- a/bot/scenes/benchmark-scene.js
+++ b/bot/scenes/benchmark-scene.js
@@ -664,10 +664,9 @@
     switch (action) {
       case 'compare':
-        {
-          let comparisonData = await getComparisonData(ctx);
-          // Rest of the code
-        }
+        const comparisonData = await getComparisonData(ctx);
+        // Rest of the code
+        break;

--- a/services/analytics/expense-benchmarking-service.js
+++ b/services/analytics/expense-benchmarking-service.js
@@ -109,13 +109,12 @@
     switch (period) {
       case 'daily':
-        {
-          let startDate = moment().startOf('day');
-          let endDate = moment().endOf('day');
-          // Rest of the code
-        }
+        const startDate = moment().startOf('day');
+        const endDate = moment().endOf('day');
+        // Rest of the code
+        break;

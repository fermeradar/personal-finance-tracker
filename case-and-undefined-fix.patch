--- a/bot/telegram-bot-command-handlers.js
+++ b/bot/telegram-bot-command-handlers.js
@@ -2,6 +2,7 @@
 const { Telegraf } = require('telegraf');
 const logger = require('../core/logger-utility');
 const { pool } = require('../config/database');
+const { detectLanguage } = require('../services/localization/language-handling-service');
+let userLanguage = 'en';  // Default language

--- a/services/analytics/expense-benchmarking-service.js
+++ b/services/analytics/expense-benchmarking-service.js
@@ -108,11 +108,12 @@
 
 function calculateDateRange(period) {
+  let startDate, endDate;
   switch (period) {
     case 'week':
-      let startDate = moment().subtract(1, 'week').startOf('day');
-      let endDate = moment().endOf('day');
+      startDate = moment().subtract(1, 'week').startOf('day');
+      endDate = moment().endOf('day');
       break;
     // ... rest of the cases
   }
   return { startDate, endDate };
 }

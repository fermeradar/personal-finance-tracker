--- a/bot/admin-handlers.js
+++ b/bot/admin-handlers.js
@@ -4,7 +4,7 @@
 const { pool } = require('../config/database');
 const logger = require('../core/logger-utility');
 const { validateAdmin } = require('../core/security-middleware');
-const fs = require('fs');
+const _fs = require('fs');
 
 // ... existing code ...
-async function handleBulkDelete(ctx, expenses, index) {
+async function handleBulkDelete(ctx, expenses, _index) {

--- a/bot/scenes/add-expense-scene.js
+++ b/bot/scenes/add-expense-scene.js
@@ -2,7 +2,7 @@
 const { Scenes } = require('telegraf');
 const logger = require('../../core/logger-utility');
 const { pool } = require('../../config/database');
-const currencyConverter = require('../../services/core/currency-conversion-service');
+const _currencyConverter = require('../../services/core/currency-conversion-service');

--- a/bot/scenes/benchmark-scene.js
+++ b/bot/scenes/benchmark-scene.js
@@ -66,7 +66,7 @@
 async function handleBenchmarkStart(ctx) {
   try {
     const chatId = ctx.chat.id;
-    const userId = ctx.from.id;
+    const _userId = ctx.from.id;

--- a/bot/scenes/receipt-processing-scene.js
+++ b/bot/scenes/receipt-processing-scene.js
@@ -200,7 +200,7 @@
 async function handleReceiptUpload(ctx) {
   try {
     const chatId = ctx.chat.id;
-    const userId = ctx.from.id;
+    const _userId = ctx.from.id;

// ... Continue with similar changes for other files
